fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sqlalchemy[asyncio]>=2.0.25
aiosqlite>=0.19.0
alembic>=1.13.0
pydantic-settings>=2.1.0
passlib[bcrypt]>=1.7.4
bcrypt<4.1
python-jose[cryptography]>=3.3.0
strawberry-graphql[fastapi]>=0.217.0
httpx>=0.26.0
pyyaml>=6.0
python-multipart>=0.0.6
asyncssh>=2.14.0
# Actions secrets are libsodium sealed boxes on GitHub; the emulator
# serves the repository public key and opens what clients seal to it.
# NOTE: this file is what the container image installs. pyproject.toml
# lists the same dependencies for local work — a package added to only
# one of them passes tests and then crashes the image at import.
pynacl>=1.5.0
supervisor>=4.2.5

# Dev dependencies
pytest>=7.4.0
pytest-asyncio>=0.23.0
